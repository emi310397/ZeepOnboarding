import ResponseCommand from "../../Commands/ResponseCommand";

export default class NewUserUseCase {
    public static execute = async (command) => {
        //Todo



        return new ResponseCommand(status, object);
    }
}